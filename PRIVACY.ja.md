# プライバシーポリシー

最終更新: 2026-09-14

## 要約

Completion Sound for ChatGPT & Codex は、個人情報、会話内容、ソースコード、
プロンプト、Codex出力、閲覧履歴、利用分析データを**収集・販売・共有・
アップロード・外部送信しません**。

解析SDK、テレメトリエンドポイント、広告SDK、アカウント機能、
リモートコード読み込み機能はありません。

## ブラウザ拡張

ブラウザ版は以下の動作だけを行います。

- `https://chatgpt.com/*` 上だけで動作
- ChatGPTが生成中かどうかを判定するため、ページUI状態をローカル監視
- 会話本文を分析・保存目的で読み取ったり外部送信したりしない
- ON/OFF、音の種類、音量、内部タイミング値だけを `chrome.storage.local` に保存
- `offscreen` 権限は、完了後に同梱WAVを再生するためだけに使用
- 拡張自身からのネットワーク通信なし

## VS Code拡張

VS Code版は以下の動作を行います。

- VS CodeのローカルUI Extension Hostで動作
- 通常 `~/.codex/sessions` 配下にあるCodexのJSONLセッションファイルを
  読み取り専用で監視
- 追記されたJSONレコードから、VS Code由来セッションとタスク完了イベントを判定
- Codexセッションファイルを変更しない
- セッション内容をアップロード・外部送信・独自保存しない
- 設定値だけをVS Code拡張の `globalState` に保存
- 通知音量を反映した同梱WAVのローカルコピーをVS Codeの拡張用
  global storageディレクトリへ保存
- 拡張自身からのネットワーク通信なし

## データ保持

本プロジェクトはサーバーを運用しないため、サーバー側のデータ保持はありません。

ローカル設定はブラウザ／VS Codeのプロファイルまたは拡張データが削除されるまで
端末上に残ります。

## 第三者

拡張はOpenAI、開発者、広告事業者、解析事業者、その他第三者へデータを送信しません。
ChatGPTおよびCodex本体が通常動作としてOpenAIと通信する部分は、本拡張の管理対象外です。

## 変更

本ポリシーに重要な変更がある場合はリポジトリとリリースノートに記載します。

## 連絡先

公開設定後のGitHubリポジトリのIssueを利用してください。
