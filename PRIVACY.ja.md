# プライバシーポリシー

最終更新: 2026-09-14

## 要約

Completion Sound for ChatGPT & Codex は、個人情報、会話内容、ソースコード、
プロンプト、Codex出力、閲覧履歴、利用分析データを**収集・販売・共有・
アップロード・外部送信しません**。

解析SDK、テレメトリエンドポイント、広告SDK、アカウント機能、
リモートコード読み込み機能はありません。

## ブラウザ拡張

ブラウザ版は以下の動作だけを行います。

- `https://chatgpt.com/*` 上だけで動作
- ChatGPTが生成中かどうかを判定するため、ページUI状態をローカル監視
- 会話本文を分析・保存目的で読み取ったり外部送信したりしない
- ON/OFF、音の種類、音量、内部タイミング値だけを `chrome.storage.local` に保存
- `offscreen` 権限は同梱WAVのローカル再生だけに使用
- 拡張自身からのネットワーク通信なし

## VS Code拡張

VS Code版は以下の動作を行います。

- VS CodeのローカルUI Extension Hostで動作
- ローカルのCodex DB `~/.codex/logs_2.sqlite` を読み取り専用で開く
- Codexの `logs` テーブルに追加された新規行を監視し、
  App Serverのローカルライフサイクルログから
  `turn/started` / `turn/completed` を判定
- Codex DBを変更しない
- Codexログ内容をアップロード・外部送信・独自保存しない
- ON/OFF、通知音、音量の設定は通常のVS Code設定に保存
- 通知音量を反映した同梱WAVのローカルコピーをVS Codeの
  global storageディレクトリへ保存
- 複数のVS Codeウィンドウによる重複通知を防ぐため、
  プロセス情報だけを含む小さな一時ロックファイルをローカルに作成
- 拡張自身からのネットワーク通信なし

## データ保持

本プロジェクトはサーバーを運用しないため、サーバー側のデータ保持はありません。

ローカルのVS Code／ブラウザ設定、生成された音声キャッシュ、一時ロックファイルは、
関連するプロファイル／設定や拡張データの削除、または通常の一時ファイル整理まで
端末上に残る場合があります。

## 第三者

拡張はOpenAI、開発者、広告事業者、解析事業者、その他第三者へデータを送信しません。
ChatGPTおよびCodex本体が通常動作としてOpenAIと通信する部分は、
本拡張の管理対象外です。

## 変更

本ポリシーに重要な変更がある場合はリポジトリとリリースノートに記載します。

## 連絡先

機密性のない質問・不具合は公開Issueを利用してください。
セキュリティ脆弱性はGitHubリポジトリの Private vulnerability reporting を
利用して非公開で報告してください。
